server:
  port: 9002
spring:
  application:
    name: tensquare-base
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.233.128:3306/tensquare_base?characterEncoding=utf-8&useSSL=false
    username: root
    password: root


    jpa:
      database: MySql
      show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka/
    instance:
      prefer‐ip‐address: true