server:
  port: 9010
spring:
  application:
    name: tensquare-friend #指定服务名
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.233.128:3306/tensquare_friend?characterEncoding=utf-8&useSSL=false
    username: root
    password: root
  jpa:
    database: MySQL
    show-sql: true
jwt:
  config:
    key: sakura
    ttl: 3600000
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka/
    instance:
      prefer‐ip‐address: true